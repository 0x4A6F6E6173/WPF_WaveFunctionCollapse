<Window x:Class="WPF_WaveFunctionCollapse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_WaveFunctionCollapse"
        mc:Ignorable="d"
        Title="Wave Function Collapse" SizeToContent="WidthAndHeight">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel>
            <Label> Construct thingy</Label>
        </StackPanel>

        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="5">
            <Canvas Name="DrawArea" ClipToBounds="True" Width="800" Height="800" Background="SteelBlue">
                
            </Canvas>
        </Border>
    </Grid>
</Window>
